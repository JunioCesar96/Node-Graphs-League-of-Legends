1.Comandos — Referência aos testes de parameterValueInput.test.ts além de leagueBinScene.test.ts.

2.Tabela “Estrutura útil” — Novas linhas para useSceneHistory (ordem e valores), parameterValueInput.ts, ParameterValueInput.tsx, ParameterItem.tsx, NodeCard.tsx e NodeInspector.tsx.

3.Nova subsecção “Cartão de nó e inspector (parâmetros)” — Cobre:

validação por tipo, bloqueio de entrada inválida e mensagem temporária no title do input;
reordenação pelo nome no cartão e por arrasto com ícone de grelha no inspector;
política de cursores no cartão.
A limitação sobre conversão binária e motor do grafo mantém-se logo abaixo.
